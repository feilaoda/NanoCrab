已做完，`/git ci` 现在会把 `.codex_last_message.txt` 用作默认 commit message，并支持长度上限。

- 提交消息来源改为：`手动 message > .codex_last_message.txt > 变更摘要`，在 `src/router.ts:1041`、`src/router.ts:1141`
- 新增读取与截断逻辑，超长按上限裁剪：`src/router.ts:1158`、`src/router.ts:1170`
- `git commit` 改为 `-F` 文件方式，完整支持多行 message：`src/router.ts:1183`
- 回执里会标记“已截断”：`src/router.ts:1050`
- 新增可配置上限 `CODEX_COMMIT_MESSAGE_MAX_CHARS`（默认 10000）：`src/config.ts:52`
- 文档已同步：`README.md:31`、`README.md:129`

校验：
- 跑了 `npm run -s typecheck`，仍是仓库既有报错（`src/store/db.ts`、`src/plugins/isolate_runtime.ts`），本次改动未引入新报错。

你如果要改上限，直接在 `.env` 里设：`CODEX_COMMIT_MESSAGE_MAX_CHARS=5000`（或你希望的值）。